language: c
compiler: gcc
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq gnome-common libglib2.0-dev gobject-introspection gtk-doc
script: ./autogen.sh && ./configure && make
